# Paywall 功能實施總結 - Quick Reference

## 🎯 實施的三大核心功能

### 1. 📸 **CameraScreen - 掃描限制與阻斷**
```
免費用戶掃描流程：
┌─────────────────────────────────────────┐
│ 第 1-3 次掃描 → ✅ 正常使用              │
│ 第 4-5 次掃描 → ⚠️  Alert 警告「即將用完」│
│ 第 6 次嘗試   → 🚫 Modal 彈出，阻止掃描  │
└─────────────────────────────────────────┘

Modal 內容：
┌────────────────────────────────┐
│  🔒  已達免費掃描上限            │
│                                │
│  您已使用完 5 次免費掃描         │
│  升級至 Premium 享受：          │
│                                │
│  ✓ 無限次掃描                  │
│  ✓ 進階健康分析                │
│  ✓ 無廣告體驗                  │
│                                │
│  [  升級 Premium  ]            │
│  [   稍後再說    ]             │
└────────────────────────────────┘
```

**檔案：** `src/screens/CameraScreen.tsx`  
**關鍵函數：** `checkScanLimit()`, `takePicture()`, `pickImage()`

---

### 2. 👤 **ProfileScreen - 升級卡片與會員狀態**

#### 未訂閱用戶看到：
```
┌────────────────────────────────────────┐
│ 📱 LabelX                       ⚙️     │
└────────────────────────────────────────┘
┌────────────────────────────────────────┐
│  👤  用戶名                             │
│                                        │
│  掃描次數   平均評分   連續天數          │
│     5         85        3             │
└────────────────────────────────────────┘

┌────────────────────────────────────────┐
│ ✨ 升級至 Premium                  →   │
│                                        │
│ 無限次掃描 • 進階分析 • 專屬徽章        │
│                                        │
│ [ 限時優惠 50% OFF ]                   │
└────────────────────────────────────────┘
```

#### Premium 用戶看到：
```
┌────────────────────────────────────────┐
│  👤  用戶名  [PRO]                      │
│  Premium 會員至 2026/10/18              │
└────────────────────────────────────────┘

┌────────────────────────────────────────┐
│ ✓ Premium 會員          [ 6個月 ]      │
│                                        │
│ 會員到期日         2026/10/18          │
│ 掃描次數            無限制 ∞            │
│                                        │
│ [     管理訂閱     ]                   │
└────────────────────────────────────────┘
```

**檔案：** `src/screens/ProfileScreen.tsx`  
**關鍵組件：** Premium 升級卡、會員狀態卡、PRO 徽章

---

### 3. 🏠 **HomeScreen - 掃描額度橫幅**

#### 免費用戶（充足）：
```
┌────────────────────────────────────────┐
│          LabelX                        │
│     Label Inspection                   │
└────────────────────────────────────────┘
┌────────────────────────────────────────┐
│ 🔍 剩餘 5/5 次免費掃描              →  │
│ 升級至 Premium 解鎖無限掃描             │
└────────────────────────────────────────┘
    ↑ 綠色背景 (#D1FAE5)
```

#### 免費用戶（即將用完）：
```
┌────────────────────────────────────────┐
│ ⚠️  剩餘 2/5 次免費掃描              →  │
│ 升級至 Premium 解鎖無限掃描             │
└────────────────────────────────────────┘
    ↑ 黃色背景 (#FEF3C7)
```

#### 免費用戶（已用完）：
```
┌────────────────────────────────────────┐
│ 🔒 免費掃描已用完                    → │
│ 升級 Premium 享受無限掃描               │
└────────────────────────────────────────┘
    ↑ 紅色背景 (#FEE2E2)
```

#### Premium 用戶：
```
┌────────────────────────────────────────┐
│ ✓ Premium 會員               [PRO]     │
│ 無限次掃描 • 進階分析                   │
└────────────────────────────────────────┘
    ↑ 綠色背景 (#D1FAE5)
```

**檔案：** `src/screens/HomeScreen.tsx`  
**關鍵邏輯：** `remainingScans` 計算、動態顏色切換

---

## 🔑 關鍵常數

```typescript
// 所有畫面共用
const FREE_SCAN_LIMIT = 5;

// RevenueCat Entitlement ID
const ENTITLEMENT_ID = "pro";

// 訂閱類型
type SubscriptionType = "3months" | "6months" | "12months";
```

---

## 📊 狀態管理架構

```
useSubscriptionStore (Zustand)
    │
    ├─ isPremium: boolean
    ├─ subscriptionType: "3months" | "6months" | "12months"
    ├─ expiresAt: Date | undefined
    │
    ↓ 被以下組件使用
    │
    ├─ CameraScreen
    │   └─ checkScanLimit() 檢查是否為 Premium
    │
    ├─ ProfileScreen
    │   └─ 顯示升級卡或會員狀態
    │
    └─ HomeScreen
        └─ 顯示掃描額度或 PRO 徽章
```

---

## 🎨 視覺設計規範

### 顏色系統
```
Primary Green:   #2CB67D  (Premium 綠、按鈕、徽章)
Background:      #FFFFFF  (主背景)

狀態顏色：
✓ 充足:         #D1FAE5   (綠色背景)
⚠️ 警告:         #FEF3C7   (黃色背景)
🚫 耗盡:         #FEE2E2   (紅色背景)

文字顏色：
Primary:        #001858   (深藍黑)
Secondary:      #6B7280   (灰色)
Tertiary:       #9CA3AF   (淺灰)
```

### 圓角規範
```
小元素:  8px   (徽章、標籤)
卡片:   20px   (ProfileScreen 卡片)
大卡片: 24px   (HomeScreen 橫幅)
Modal:  28px   (CameraScreen Modal)
```

### 間距規範
```
Section 間距:  24px (mt-6)
Card 內 padding: 16-20px
Icon 和文字間距: 8-12px
```

---

## 🔄 用戶旅程地圖

```
新用戶安裝 App
    ↓
HomeScreen: 看到「剩餘 5/5 次免費掃描」
    ↓
前往 CameraScreen 掃描第 1 次
    ↓ (第 2-3 次同樣流程)
掃描第 4 次
    ↓
Alert: 「掃描次數即將用完，還剩 2 次」
    ↓
掃描第 5 次 (最後一次)
    ↓
Alert: 「掃描次數即將用完，還剩 1 次」
    ↓
HomeScreen: 橫幅變紅「免費掃描已用完」
    ↓
嘗試第 6 次掃描
    ↓
CameraScreen: Modal 彈出 🚫
    ├─ 點擊「稍後再說」→ 關閉 Modal
    └─ 點擊「升級 Premium」
          ↓
      返回主畫面 (goBack)
          ↓
      (可選) 自動導航至 PaywallScreen
          ↓
      完成購買
          ↓
      isPremium = true
          ↓
      所有畫面更新顯示 Premium 狀態
          ↓
      無限掃描 ∞
```

---

## ⚡ 快速測試指令

### 模擬免費用戶
```typescript
// 在 App.tsx 或測試環境中
useSubscriptionStore.setState({ isPremium: false });
useFoodScanStore.setState({ scanHistory: [] }); // 0 次掃描
```

### 模擬即將用完
```typescript
useSubscriptionStore.setState({ isPremium: false });
useFoodScanStore.setState({ 
  scanHistory: Array(4).fill(mockScanResult) // 4 次掃描
});
```

### 模擬已用完
```typescript
useSubscriptionStore.setState({ isPremium: false });
useFoodScanStore.setState({ 
  scanHistory: Array(5).fill(mockScanResult) // 5 次掃描
});
```

### 模擬 Premium 用戶
```typescript
useSubscriptionStore.setState({ 
  isPremium: true,
  subscriptionType: "6months",
  expiresAt: new Date("2026-10-18")
});
```

---

## 🐛 常見問題 FAQ

### Q1: 為什麼 Modal 點擊「升級」只返回首頁？
**A:** 目前實現為 `navigation.goBack()`，可擴展為：
```typescript
onPress={() => {
  setShowScanLimitModal(false);
  navigation.navigate("Home");
  // 使用 setTimeout 確保 navigation 完成
  setTimeout(() => {
    navigation.navigate("Profile", { 
      screen: "Paywall", 
      params: { source: "scan_limit" } 
    });
  }, 300);
}}
```

### Q2: 掃描次數如何重置？
**A:** 不自動重置。免費用戶達到 5 次後永久限制，除非：
1. 購買 Premium
2. 管理員手動清除 `scanHistory`（開發測試用）

### Q3: Premium 用戶過期後會怎樣？
**A:** RevenueCat 自動處理：
- `isPremium` 變為 `false`
- UI 自動切換回免費用戶模式
- 但掃描歷史保留（可能超過 5 次）
- 建議添加「恢復購買」提示

### Q4: 如何追蹤轉換率？
**A:** 使用 RevenueCat Dashboard 的「Charts」功能：
- Conversion to Paying
- Trial Conversion
- Active Subscriptions

---

## 📁 檔案結構

```
src/
├── screens/
│   ├── CameraScreen.tsx      ✅ 掃描限制 + Modal
│   ├── ProfileScreen.tsx     ✅ 升級卡 + 會員狀態 + PRO 徽章
│   ├── HomeScreen.tsx         ✅ 掃描額度橫幅
│   └── PaywallScreen.tsx      (已完成，未修改)
│
├── state/
│   └── subscriptionStore.ts   (已完成，未修改)
│
└── api/
    └── revenue-cat-service.ts (已完成，未修改)
```

---

## ✅ 實施檢查清單

- [x] CameraScreen 掃描限制邏輯
- [x] CameraScreen 限制 Modal UI
- [x] ProfileScreen Premium 升級卡
- [x] ProfileScreen 會員狀態卡
- [x] ProfileScreen PRO 徽章
- [x] HomeScreen 掃描額度橫幅（免費用戶）
- [x] HomeScreen Premium 徽章（付費用戶）
- [x] 動態顏色切換（綠/黃/紅）
- [x] 所有畫面使用統一的 FREE_SCAN_LIMIT
- [x] 移除無效的 RevenueCat plugin 配置
- [x] App 編譯無錯誤

---

## 🚀 下一步行動

1. **RevenueCat 配置**
   - 註冊帳號並創建 Project
   - 配置 iOS/Android App
   - 創建訂閱產品
   - 設置 Offering 和 Entitlement

2. **Development Build**
   ```bash
   eas build --profile development --platform ios
   eas build --profile development --platform android
   ```

3. **真實設備測試**
   - 使用 Sandbox 帳號測試購買
   - 驗證掃描限制邏輯
   - 測試 UI 顯示切換

4. **上線準備**
   - App Store 截圖和說明
   - Google Play 商店列表
   - 設置定價和地區

---

**實施完成 ✅**  
**準備測試和部署 🚀**
