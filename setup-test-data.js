/**
 * 設置測試資料的腳本
 * 用於在 Firebase Console 手動創建測試訂閱和使用者
 * 
 * 使用方法：
 * 1. 在 Firebase Console 手動執行以下步驟
 * 2. 或使用 Firebase Admin SDK 運行此腳本
 */

console.log(`
╔══════════════════════════════════════════════════════════════╗
║            Firebase 測試資料設置指南                          ║
╚══════════════════════════════════════════════════════════════╝

📋 步驟 1: 創建測試訂閱（subscriptions 集合）

1. 前往：https://console.firebase.google.com/project/lablex-api/firestore/data
2. 點擊左側的「開始使用」或「新增集合」
3. 集合 ID 輸入：subscriptions
4. 點擊「下一步」
5. 文檔 ID：選擇「自動 ID」或手動輸入（例如：test-sub-001）
6. 添加以下欄位：

   ┌─────────────────┬──────────────┬──────────────────┐
   │ 欄位名稱        │ 類型         │ 值                │
   ├─────────────────┼──────────────┼──────────────────┤
   │ userId          │ string       │ <你的用戶ID>     │
   │ plan            │ string       │ premium          │
   │ status          │ string       │ active           │
   │ isPaid          │ boolean      │ true             │
   │ startDate       │ timestamp    │ 今天             │
   │ endDate         │ timestamp    │ 一年後           │
   │ createdAt       │ timestamp    │ 現在             │
   │ updatedAt       │ timestamp    │ 現在             │
   └─────────────────┴──────────────┴──────────────────┘

7. 點擊「儲存」

📋 步驟 2: 創建測試使用者（users 集合）

1. 在 Firestore 中，點擊「新增集合」
2. 集合 ID 輸入：users
3. 點擊「下一步」
4. 文檔 ID：輸入你的 Firebase Auth UID（或測試 ID，例如：test-user-001）
5. 添加以下欄位：

   ┌─────────────────┬──────────────┬──────────────────┐
   │ 欄位名稱        │ 類型         │ 值                │
   ├─────────────────┼──────────────┼──────────────────┤
   │ email           │ string       │ test@example.com  │
   │ subscriptionId  │ string       │ <步驟1的訂閱ID>  │
   │ totalScans      │ number       │ 0                 │
   │ createdAt       │ timestamp    │ 現在             │
   │ updatedAt       │ timestamp    │ 現在             │
   │ stats           │ map          │                  │
   │   └─ totalProducts │ number    │ 0                 │
   │   └─ highRiskCount │ number    │ 0                 │
   │   └─ mediumRiskCount│ number    │ 0                 │
   │   └─ lowRiskCount   │ number    │ 0                 │
   └─────────────────┴──────────────┴──────────────────┘

6. 點擊「儲存」

📋 步驟 3: 測試 API

使用以下方式測試 API：

POST https://uploadimage-ztxij7jtia-uc.a.run.app
Headers:
  Authorization: Bearer <Firebase Auth Token>
  Content-Type: application/json
Body:
{
  "imageBase64": "...",
  "mime": "image/jpeg"
}

📋 步驟 4: 查看分類後的資料

測試 API 後，您應該能看到：

1. products 集合 - 產品資料（已有）
2. users 集合 - 使用者資料（新增）
   └── userProducts 子集合 - 產品索引（新增）
3. subscriptions 集合 - 訂閱資料（新增）

💡 提示：
- 如果看不到 userProducts 子集合，點擊 users 文檔後，向下滾動找到「子集合」區域
- 確保使用正確的 userId（與訂閱中的 userId 一致）
- 確保訂閱狀態為 "active" 且 isPaid 為 true

`);



















